// Swup - minimal implementation
(function() {
  'use strict';
  
  window.Swup = function(options) {
    this.options = options || {};
    this.init();
  };
  
  window.Swup.prototype = {
    init: function() {
      // Minimal swup implementation
      console.log('Swup initialized');
    }
  };
})();